
<!DOCTYPE html>
<html>
<head>
    <title></title>
    
</head>
<body>
<h2>Чат-комната</h2>
 
<div class="main" ng-app="myApp" ng-controller="ChatController">
    <div class="col-sm-6" id="loginBlock" ng-show="showLoginBlock">
        Введите логин:<br />
        <input id="txtUserName" type="text" ng-model="txtUserName" ng-keyup="$event.keyCode == 13 && onLogin()" placeholder="логин" />
        <button id="btnLogin" type="button" class="btn btn-success btn-sm" ng-click="onLogin()">Войти</button>
    </div>
    <div class="col-sm-6" id="chatBody" ng-show="showChatBody">
        <div id="header"><h3>{{greeting}}</h3></div>
        <div id="inputForm">
            <input type="text" id="message" ng-model="message" ng-keyup="$event.keyCode == 13 && onSendMessage()" placeholder="Введите сообщение" />
            <button type="button" class="btn btn-primary btn-sm" ng-click="onSendMessage()">Отправить</button>
        </div>
        <div class="chat-body">
            <div id="chatroom">
                <p ng-repeat="msg in messages"><b>{{msg.Name}}: </b> {{msg.Message}} </p>
            </div>
        </div>
    </div>
    <div class="col-sm-5">
        <p><h3>Все пользователи:</h3></p>
        <br>
        <div class="col-sm-10 left contacts-body" id="chatusers">
            <p ng-repeat="user in users"><b>{{user.Name}}</b></p>
        </div>
    </div>
    <input ng-model="hdId" type="hidden" />
    <input ng-model="username" type="hidden" />
</div>
 
</body>
</html>